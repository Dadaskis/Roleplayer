{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core invoke + events.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
